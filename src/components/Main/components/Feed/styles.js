import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  feed: {
    background: "#2d4059",
    width: "100%",
    padding: "24px",
  },
}));

export default useStyles;
